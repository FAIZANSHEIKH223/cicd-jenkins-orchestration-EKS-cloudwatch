apiVersion: v1
kind: Service

metadata:
  name: practice1-service
  namespace: practice1

  labels:
    app: practice1

spec:
  type: LoadBalancer

  selector:
    app: practice1

  ports:
    - name: http

      protocol: TCP

      port: 80

      targetPort: 8501
